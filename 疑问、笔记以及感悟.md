# 疑问

1. 三个模型全都要做吗？

2. 论文当中图1的注意力图是什么意思，怎么做的？



# 笔记

## 标准transformer

非常好的问题！这些问题恰恰是从"看懂公式"到"真正理解"的关键。我会逐一详细解释，**每个问题都会用具体的数值例子**。

### 完整的编码器层

输入X，先做位置编码然后直接相加（记录残差1），layernorm，多头自注意力机制，记录残差2并且残差链接，layernorm，前馈网络，残差链接

```
输入 X (形状 [T, d])
    ↓
【步骤1】残差连接1
    ↓
【步骤2】层归一化1 (LayerNorm)
    ↓
【步骤3】多头自注意力 (Multi-Head Self-Attention)
    ↓
【步骤4】残差连接2 (与步骤1的输入相加)
    ↓
【步骤5】层归一化2 (LayerNorm)
    ↓
【步骤6】前馈网络 (FFN)
    ↓
【步骤7】残差连接3 (与步骤4的输出相加)
    ↓
输出 Y (形状 [T, d])
```

假设 `T=3, d=4`（3个位置，每个4维）

**初始输入**：

```
X = [[1, 0, -1, 2],    # 位置0
     [0, 2, 1, -1],    # 位置1  
     [1, 1, 0, 1]]     # 位置2
```

#### 步骤1：残差连接1

```
X1 = X  # 保留原始输入，用于后续相加
```

#### 步骤2：层归一化1

对每个位置，在4个维度上做归一化：

**位置0**：
```
均值 μ = (1+0-1+2)/4 = 0.5
方差 σ² = ((1-0.5)²+(0-0.5)²+(-1-0.5)²+(2-0.5)²)/4 = (0.25+0.25+2.25+2.25)/4 = 1.25
σ = √1.25 ≈ 1.118

归一化后：[(1-0.5)/1.118, (0-0.5)/1.118, (-1-0.5)/1.118, (2-0.5)/1.118]
        = [0.447, -0.447, -1.342, 1.342]
```

**位置1**：
```
均值 μ = (0+2+1-1)/4 = 0.5
σ = 1.118（同样）
归一化后：[(-0.5)/1.118, (1.5)/1.118, (0.5)/1.118, (-1.5)/1.118]
        = [-0.447, 1.342, 0.447, -1.342]
```

**位置2**：
```
均值 μ = (1+1+0+1)/4 = 0.75
方差 σ² = ((0.25)²+(0.25)²+(-0.75)²+(0.25)²)/4 = (0.0625+0.0625+0.5625+0.0625)/4 = 0.1875
σ = √0.1875 ≈ 0.433

归一化后：[(1-0.75)/0.433, (1-0.75)/0.433, (0-0.75)/0.433, (1-0.75)/0.433]
        = [0.577, 0.577, -1.732, 0.577]
```

假设没有可学习的γ,β（或都等于1），则 `X_LN1` 为上述值。

#### 步骤3：多头自注意力（简化版，单头）

**线性变换得到 Q, K, V**（假设 W_Q, W_K, W_V 都是单位矩阵）：
```
Q = X_LN1, K = X_LN1, V = X_LN1
```

**计算注意力权重**（简化，没有缩放）：
```
S = Q·K^T  # 3×3矩阵

位置0对位置0: 0.447×0.447 + (-0.447)×(-0.447) + (-1.342)×(-1.342) + 1.342×1.342 = 4.0
位置0对位置1: 0.447×(-0.447) + (-0.447)×1.342 + (-1.342)×0.447 + 1.342×(-1.342) = -3.0
位置0对位置2: 0.447×0.577 + (-0.447)×0.577 + (-1.342)×(-1.732) + 1.342×0.577 = 2.5

S = [[4.0, -3.0, 2.5],
     [-3.0, 4.0, -3.0],
     [2.5, -3.0, 4.0]]
```

**Softmax（对每行）**：
```
行0: exp(4)=54.6, exp(-3)=0.05, exp(2.5)=12.18
     总和=66.83
     A[0,:] = [54.6/66.83, 0.05/66.83, 12.18/66.83] = [0.817, 0.001, 0.182]

行1: [0.001, 0.998, 0.001]
行2: [0.182, 0.001, 0.817]
```

**加权求和得到输出**：
```
O[0] = 0.817×V[0] + 0.001×V[1] + 0.182×V[2]
     = 0.817×[0.447, -0.447, -1.342, 1.342] 
     + 0.001×[-0.447, 1.342, 0.447, -1.342]
     + 0.182×[0.577, 0.577, -1.732, 0.577]
     = [0.365, -0.365, -1.096, 1.096] + [-0.0004, 0.001, 0.0004, -0.001] + [0.105, 0.105, -0.315, 0.105]
     = [0.470, -0.259, -1.411, 1.200]

O[1] = [0.001×V0 + 0.998×V1 + 0.001×V2] ≈ V1 = [-0.447, 1.342, 0.447, -1.342]
O[2] = [0.182×V0 + 0.001×V1 + 0.817×V2] = [0.558, 0.558, -1.538, 0.558]
```

#### 步骤4：残差连接2

```
X2 = X1 + O  # 原始输入 + 注意力输出
X2[0] = [1, 0, -1, 2] + [0.470, -0.259, -1.411, 1.200] = [1.470, -0.259, -2.411, 3.200]
X2[1] = [0, 2, 1, -1] + [-0.447, 1.342, 0.447, -1.342] = [-0.447, 3.342, 1.447, -2.342]
X2[2] = [1, 1, 0, 1] + [0.558, 0.558, -1.538, 0.558] = [1.558, 1.558, -1.538, 1.558]
```

#### 步骤5：层归一化2

同步骤2，对X2做层归一化 → `X_LN2`

#### 步骤6：前馈网络（FFN）

```
FFN(x) = ReLU(x·W1 + b1)·W2 + b2
```

具体例子（假设W1是4×8，W2是8×4，简化）：
```
对位置0：x = X_LN2[0] (假设为 [0.5, -0.3, 0.8, 0.2])
中间层：h = ReLU(x·W1) = ReLU([0.5, -0.3, 0.8, 0.2]·W1)
       假设结果为 [1.2, -0.5, 0.7, 0.1, -0.2, 0.9, 0.3, 0.4]
       ReLU后 = [1.2, 0, 0.7, 0.1, 0, 0.9, 0.3, 0.4]
输出：FFN(x) = h·W2 (假设结果为 [0.3, -0.1, 0.5, 0.2])
```

#### 步骤7：残差连接3

```
Y = X2 + FFN(X_LN2)
```

**为什么要残差连接？**

- 防止梯度消失，让信息可以直接流过
- 如果层没学到东西，输出 ≈ 输入

**为什么要层归一化？**

- 稳定训练，让每层的输入分布稳定
- 避免内部协变量偏移

**FFN的作用？**
- 对每个位置独立做非线性变换
- 增加模型的表达能力（注意力是线性加权，FFN引入非线性）

---

## 问题3：Transformer如何反向传播？

### 3.1 反向传播的基本概念

**前向传播**：输入 → 逐层计算 → 输出 → 计算损失
**反向传播**：损失 → 逐层求梯度 → 更新参数

**核心数学**：链式法则（Chain Rule）

### 3.2 用具体例子说明

假设最简单的模型：`y = w·x + b`，损失为 `L = (y - y_target)²`

**前向**：
```
x=2, w=3, b=1
y = 3×2 + 1 = 7
L = (7-5)² = 4  # 假设目标=5
```

**反向**（求导数）：
```
∂L/∂y = 2×(7-5) = 4
∂y/∂w = x = 2
∂y/∂b = 1

∂L/∂w = ∂L/∂y × ∂y/∂w = 4 × 2 = 8
∂L/∂b = ∂L/∂y × ∂y/∂b = 4 × 1 = 4
```

更新参数（学习率0.1）：
```
w_new = 3 - 0.1×8 = 2.2
b_new = 1 - 0.1×4 = 0.6
```

### 3.3 Transformer中的反向传播

#### 每个模块的梯度

**1. Softmax的反向传播**

前向：`s_i = exp(z_i) / Σ exp(z_j)`

反向：如果损失对 `s_i` 的梯度是 `g_i`，则对 `z_k` 的梯度：
```
∂L/∂z_k = Σ_i (∂L/∂s_i) × (∂s_i/∂z_k)
其中 ∂s_i/∂z_k = s_i × (δ_{ik} - s_k)
```

**具体数值例子**：
```
z = [2, 1, 0.5]
s = softmax(z) = [0.59, 0.22, 0.19]
假设损失对s的梯度 g = [0.1, 0.3, 0.5]

对z0的梯度：
∂L/∂z0 = g0×s0×(1-s0) + g1×(-s1×s0) + g2×(-s2×s0)
        = 0.1×0.59×0.41 + 0.3×(-0.22×0.59) + 0.5×(-0.19×0.59)
        = 0.0242 - 0.0389 - 0.0561 = -0.0708
```

**2. 矩阵乘法（线性层）的反向**

前向：`y = x·W`

反向：如果损失对y的梯度是 `∂L/∂y`，则：
```
∂L/∂x = (∂L/∂y)·W^T
∂L/∂W = x^T·(∂L/∂y)
```

**具体数值例子**：
```
x = [1, 2], W = [[3, 4], [5, 6]]  # 2×2
y = x·W = [1×3+2×5, 1×4+2×6] = [13, 16]
假设 ∂L/∂y = [0.1, 0.2]

∂L/∂x = [0.1, 0.2]·W^T = [0.1, 0.2]·[[3,5],[4,6]] 
       = [0.1×3+0.2×4, 0.1×5+0.2×6] = [1.1, 1.7]

∂L/∂W = x^T·[0.1, 0.2] = [[1],[2]]·[0.1, 0.2] = [[0.1, 0.2], [0.2, 0.4]]
```

**3. 层归一化的反向**

前向：`y = (x-μ)/σ × γ + β`

反向需要求对x的梯度（会用到链式法则对μ和σ也求导）。

**4. 整个链**

```
损失 L
   ↓
∂L/∂Y (最后一层输出)
   ↓
∂L/∂X_LN2 (FFN输入) = ∂L/∂Y × ∂Y/∂FFN × ∂FFN/∂X_LN2
   ↓
∂L/∂O (注意力输出) = ∂L/∂X_LN2 × ∂X_LN2/∂O (残差连接梯度=1)
   ↓
∂L/∂Q, ∂L/∂K, ∂L/∂V (通过注意力反向)
   ↓
∂L/∂W_Q, ∂L/∂W_K, ∂L/∂W_V (参数梯度)
   ↓
更新参数
```

## amp自动混合精度训练（Automatic Mixed Precision）

减少显存占用。AMP会自动判断哪些操作适合低精度，用float16，哪些操作保留高精度，用float32。低精度可以减少显存，加快矩阵乘法

with torch.cuda.amp.autocast(enabled=amp):                                                                                                                    
      logits = model(x)                                                                                                                                         
      loss = criterion(logits.reshape(-1, vocab_size), y.reshape(-1))

在这个 with 块里，PyTorch 自动选择合适的数据类型执行计算。

scaler = torch.cuda.amp.GradScaler(enabled=amp)

先把loss放大很多倍，反向传播得到放大的梯度，更新参数前再把梯度缩回去。小梯度在计算过程中不容易变成0。

optimizer.zero_grad()                                                                                                                                         
  scaler.scale(loss).backward()                                                                                                                                 
  scaler.step(optimizer)                                                                                                                                        
  scaler.update()

AFT里面显存大的地方是scores[B, T, T, D]。

## activation checkpointing

可以进一步省显存，但是会更慢。前向时少保存中间激活，反向时重新计算一边前向。

## gradient accumulation

用多个小batch累积梯度，模拟大batch



## DDP Distributed Data Parallel分布式训练

每张卡算自己的loss和梯度

DDP自动all-reduce梯度

每张卡执行optimizer.steep()

最终所有卡上的模型参数保持一致

# 感悟

第一天看完论文后面几天就都在写代码了

感觉实现原理的过程以及学习transformer中还是比较有意思的，有一种“哦原来这个地方是这样子，原来也不难嘛“的感觉。但是后面的数据处理、训练推理脚本编写和内存啊、多卡多进程的部分就感觉比较无趣而且很折磨
